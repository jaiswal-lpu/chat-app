
services:
  backend:
    build: ./backend
    ports:
      - '5000:5001'
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    ports:
      - '3000:5173'
    depends_on:
      - backend

